{
  "version": "1.0",
  "engine": "vela",
  "language": "ko",
  "sandbox": {
    "enabled": true
  },
  "gate_guard": {
    "enabled": true
  },
  "gate_policy": {
    "chain_operator": "block",
    "web_in_write": "block",
    "researcher_scope": "block",
    "event_log": true
  },
  "review_gate": {
    "enabled": true,
    "validation_rounds": 1,
    "steps": ["execute"]
  },
  "scale_guard": {
    "enabled": true,
    "thresholds": {
      "downgrade_signals_min": 2,
      "upgrade_signals_min": 2
    }
  },
  "changeSurface": {
    "enabled": true,
    "excludePaths": [
      "node_modules",
      ".git",
      ".gsd",
      ".vela/artifacts",
      "package-lock.json",
      "yarn.lock",
      "pnpm-lock.yaml"
    ],
    "minTokenLength": 3
  },
  "cache": {
    "__doc__": "v7.2 M1/M3 — Prompt caching policy. ttl='1h' requires ENABLE_PROMPT_CACHING_1H=1 env var to be exported before launching claude; session-start hook warns when the env var is missing. read_cache is observational (PreToolUse measurement hook) — set enabled=false to disable the read-cache.jsonl logging.",
    "enabled": true,
    "ttl": "1h",
    "read_cache": {
      "enabled": true,
      "warn_threshold": 4
    }
  },
  "execution": {
    "__doc__": "v7.2 Phase B — Parallelism & isolation for agent spawns. Defaults keep V7 serial behavior. parallelism=true enables reviewer+verifier parallel (M4) and research 3-perspective parallel (M5). isolation='worktree' runs executor in a .vela/worktrees/{slug}/ git worktree (M6). background_post_steps=true runs learning/diff-summary with run_in_background:true (M7).",
    "parallelism": false,
    "isolation": "inline",
    "background_post_steps": false
  },
  "models": {
    "__doc__": "v7.2 M2 — Per-role model routing. PM reads `recommended_model` from `vela-engine state` and passes it as the `model` field to Agent() spawns. 'default' applies when a step has no explicit mapping. Valid values: 'sonnet' | 'opus' | 'haiku' | 'inherit'.",
    "default": "sonnet",
    "research": "opus",
    "plan": "opus",
    "plan_check": "haiku",
    "execute": "sonnet",
    "verify": "haiku",
    "review": "sonnet",
    "learning": "haiku",
    "diff_summary": "haiku",
    "analyze": "sonnet"
  }
}
